package main.java.models;

/**
 * Type of object (e.g., picture, video or dir).
 */
public enum Type {
    /**
     * Directory.
     */
    DIR;

    public static Type getType() {
        return DIR;
    }
}
